using TMPro;
using UnityEngine;

public class FlightStateUI : MonoBehaviour
{
    /*[Header("State Print Text")]
    [SerializeField] private TextMeshProUGUI stateText;

    [Header("Target State Machine Object")]
    [SerializeField] private FlightStateController flightStateController;

    public void Update()
    {
        IState currentState = flightStateController.CurrentState;

        if (currentState == null)
        {
            stateText.text = "Unknown";
            return;
        }

        stateText.text = $"Current: {currentState.GetType().Name.Replace("State", ""),6}";
    }*/
}
